import { CategoryEmojiPipe } from './category-emoji.pipe';

describe('CategoryEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
